//nikita

import 'package:flutter/material.dart';

class WritingPrac extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(''),
      ),
      body: Center(
        child: Text(
          'cos',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}