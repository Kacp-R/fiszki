import 'package:flutter/material.dart';

class Flashcards extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('dos stream'),
      ),
      body: Center(
        child: Text(
          'cos',
          style: TextStyle(fontSize: 24.0),
        ),
      ),
    );
  }
}